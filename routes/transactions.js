const express = require('express'),
      router = express.Router(),
      conn = require('../controllers/transactionController')
//
//
// router.get('/', conn.findAllTransactions)
//
// router.post('/', conn.createTransaction)
//
// router.put('/:id', conn.updateTransaction)
//
// router.delete('/:id', conn.deleteTransaction)
//
//
module.exports = router;